#include <bits/stdc++.h>

using namespace std;

int main(){         
    int together, separate;
    cin >> together >> separate;
    string together2[together][together];
    string separate2[separate][separate];
    for(int i = 0; i < together; i++){
        for(int v = 0; v < together; v++){
            cin >> together2[i][v];
            cin >> separate2[i][v];
        }
    }
    int input;
    cin >> input;
    string name1,name2,name3;
    for(int i = 0; i < input;i++){
                

    }
    
     
    return 0;
    }